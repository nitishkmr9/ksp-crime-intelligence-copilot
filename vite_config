
# Create vite.config.ts
vite_config = '''import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'path'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
      '@components': path.resolve(__dirname, './src/components'),
      '@hooks': path.resolve(__dirname, './src/hooks'),
      '@utils': path.resolve(__dirname, './src/utils'),
      '@data': path.resolve(__dirname, './src/data'),
      '@styles': path.resolve(__dirname, './src/styles'),
    },
  },
  build: {
    outDir: 'dist',
    sourcemap: true,
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['react', 'react-dom'],
          charts: ['recharts'],
          icons: ['lucide-react'],
        },
      },
    },
  },
  server: {
    port: 3000,
    open: true,
  },
})
'''

with open(f"{base_dir}/vite.config.ts", "w") as f:
    f.write(vite_config)

# Create tailwind.config.js
tailwind_config = '''/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        ksp: {
          base: '#0A0E13',
          panel: '#121820',
          'panel-alt': '#161D26',
          border: '#212B36',
          text: '#E7EDF3',
          muted: '#7C8896',
          cyan: '#22D3C8',
          amber: '#F5A623',
        },
      },
      fontFamily: {
        mono: ['ui-monospace', '"IBM Plex Mono"', 'SFMono-Regular', 'Menlo', 'monospace'],
        sans: ['Inter', 'ui-sans-serif', 'system-ui', '-apple-system', 'sans-serif'],
      },
      animation: {
        'ksp-blink': 'kspBlink 1.1s infinite ease-in-out',
      },
      keyframes: {
        kspBlink: {
          '0%, 80%, 100%': { opacity: '0.25' },
          '40%': { opacity: '1' },
        },
      },
    },
  },
  plugins: [],
}
'''

with open(f"{base_dir}/tailwind.config.js", "w") as f:
    f.write(tailwind_config)

# Create postcss.config.js
postcss_config = '''export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
'''

with open(f"{base_dir}/postcss.config.js", "w") as f:
    f.write(postcss_config)

print("✅ vite.config.ts, tailwind.config.js, postcss.config.js created")
